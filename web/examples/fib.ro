fun fib(n) {
    // return n < 2 ? n : fib(n-1) + fib(n-2)
    if (n < 2)
        return n
    else
        return fib(n - 1) + fib(n - 2)
}

for (n in 2..=20)
    printl('fib(' + n + ') = ' + fib(n))
